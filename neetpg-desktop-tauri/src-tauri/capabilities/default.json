{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal surface the NEET PG webview is allowed to touch. All network and disk access happens in the Rust core — the webview can only invoke the app's own commands, listen to its events, and manage its own window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "core:window:allow-start-dragging",
    "core:event:default",
    "global-shortcut:default",
    "autostart:default"
  ]
}
