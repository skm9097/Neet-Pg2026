name: neet_quiz_app
description: NEET-PG 2026 AI-powered quiz app with voice feedback.

publish_to: 'none'

version: 1.2.0+3

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_tts: ^4.0.2
  google_generative_ai: ^0.4.3
  shared_preferences: ^2.2.3
  speech_to_text: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
